import { Course, User } from './types';

export const INITIAL_USERS: User[] = [];

export const INITIAL_COURSES: Course[] = [];

export const INITIAL_POSTS: Array<{
  id: string;
  title: string;
  body: string;
}> = [];
